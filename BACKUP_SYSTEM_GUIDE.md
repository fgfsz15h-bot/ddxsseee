# 💾 دليل نظام النسخ الاحتياطي

هذا الدليل يشرح نظام النسخ الاحتياطي التلقائي الجديد الذي يحمي بيانات المشتركين من الضياع.

---

## ✨ المميزات الجديدة

### 1. النسخ الاحتياطي التلقائي المحلي

- **يتم تلقائياً** عند كل عملية حفظ للمشتركين
- يتم الاحتفاظ بـ **آخر 10 نسخ** احتياطية
- يتم الحفظ في مجلد `backups/`
- نسخة "latest" دائماً متوفرة للاسترجاع السريع

### 2. الاسترجاع التلقائي

- إذا فُقد ملف `data/users.json`، البوت يسترجع تلقائياً من آخر نسخة احتياطية
- لا حاجة لتدخل يدوي في معظم الحالات

### 3. أمر النسخ الاحتياطي اليدوي

- أمر جديد: `/backup` (للأدمن فقط)
- يرسل لك نسخة احتياطية مباشرة في تيليجرام
- يمكنك حفظها على جهازك أو في السحابة

### 4. حماية متعددة الطبقات

| الطبقة | الموقع | التحديث |
|--------|--------|---------|
| الأساسي | `data/users.json` | فوري |
| النسخة الأخيرة | `backups/users_latest.json` | فوري |
| النسخ المؤرخة | `backups/users_backup_YYYYMMDD_HHMMSS.json` | فوري |
| نسخة تيليجرام | رسالة للأدمن | يدوي بأمر `/backup` |

---

## 🚀 كيفية الاستخدام

### للمستخدمين العاديين

لا شيء! كل شيء يعمل تلقائياً في الخلفية. فقط استخدم البوت بشكل طبيعي.

### للأدمن

#### الحصول على نسخة احتياطية

```
/backup
```

سيرسل لك البوت ملف JSON يحتوي على جميع معرفات المشتركين.

**احفظ هذا الملف في:**
- جهازك المحلي
- Google Drive
- Dropbox
- أي مكان آمن

#### التحقق من عدد المشتركين

```
/users
```

---

## 🔄 استرجاع النسخة الاحتياطية

### الطريقة الأولى: تلقائياً (الأسهل!)

البوت يسترجع تلقائياً من `backups/users_latest.json` إذا فُقد الملف الأساسي.

**لا حاجة لأي شيء!**

### الطريقة الثانية: يدوياً باستخدام السكربت

إذا أردت استرجاع نسخة احتياطية قديمة:

```bash
python restore_backup.py
```

سيعرض لك قائمة بجميع النسخ المتوفرة:

```
📋 النسخ الاحتياطية المتوفرة (5):

1. users_latest.json
   👥 المشتركين: 150
   📅 التاريخ: 2026-02-19 15:30:00
   💾 الحجم: 1024 بايت

2. users_backup_20260219_153000.json
   👥 المشتركين: 150
   📅 التاريخ: 2026-02-19 15:30:00
   💾 الحجم: 1024 بايت

...

اختر رقم النسخة للاسترجاع (أو 0 للإلغاء):
```

### الطريقة الثالثة: من ملف تيليجرام

إذا حصلت على نسخة احتياطية من أمر `/backup`:

1. حمّل الملف من تيليجرام
2. ضعه في مجلد `data/` باسم `users.json`
3. أعد تشغيل البوت

---

## 📁 هيكل الملفات

```
telegram-bot/
├── main.py                 # الكود الأساسي (محدّث)
├── requirements.txt
├── Procfile
├── data/
│   ├── users.json         # الملف الأساسي
│   └── gift_state.json
└── backups/
    ├── users_latest.json                    # آخر نسخة دائماً
    ├── users_backup_20260219_153000.json   # نسخة مؤرخة
    ├── users_backup_20260219_140000.json
    └── ...                                  # آخر 10 نسخ
```

---

## 🔧 التثبيت والتحديث

### إذا كان البوت يعمل بالفعل

1. **استبدل ملف `main.py`** بالنسخة الجديدة
2. **ارفع التحديث على GitHub:**
   ```bash
   git add main.py .gitignore
   git commit -m "Add automatic backup system"
   git push
   ```
3. **Railway سيعيد النشر تلقائياً**
4. **لا حاجة لتغيير المتغيرات البيئية**

### إذا كان مشروع جديد

اتبع نفس خطوات النشر السابقة، الملفات جاهزة.

---

## ⚠️ ملاحظات مهمة

### 1. مجلد backups/ على Railway

**المشكلة:** Railway قد يحذف الملفات عند إعادة النشر.

**الحل:**
- استخدم أمر `/backup` بشكل دوري (مثلاً كل أسبوع)
- احفظ النسخة على جهازك أو السحابة
- النسخ المحلية على Railway مفيدة للاسترجاع السريع فقط

### 2. حجم الملفات

النسخ الاحتياطية صغيرة جداً (عادة أقل من 10 KB لكل 1000 مشترك).

### 3. الخصوصية

- الملفات تحتوي فقط على معرفات المشتركين (أرقام)
- لا تحتوي على أسماء أو معلومات شخصية
- مع ذلك، احفظها في مكان آمن

---

## 🆕 الأوامر الجديدة

| الأمر | الوصف | من يستخدمه |
|-------|--------|------------|
| `/backup` | إرسال نسخة احتياطية | الأدمن فقط |

الأوامر القديمة تعمل كما هي بدون تغيير.

---

## 🧪 اختبار النظام

### اختبار 1: النسخ الاحتياطي التلقائي

1. أضف مشترك جديد (اضغط `/start` من حساب آخر)
2. تحقق من وجود ملف `backups/users_latest.json`
3. يجب أن يحتوي على المشترك الجديد

### اختبار 2: الاسترجاع التلقائي

1. احذف ملف `data/users.json` (للاختبار فقط!)
2. أعد تشغيل البوت
3. استخدم `/users` - يجب أن يظهر العدد الصحيح
4. تحقق من أن `data/users.json` تم استعادته

### اختبار 3: أمر النسخ الاحتياطي

1. استخدم `/backup` في البوت
2. يجب أن يرسل لك ملف JSON
3. افتح الملف وتحقق من المحتوى

---

## 🐛 استكشاف الأخطاء

### البوت لا يرسل نسخة احتياطية

**الأسباب المحتملة:**
- `ADMIN_ID` غير صحيح
- استخدم `/me` للتحقق من معرفك
- تأكد من أنك الأدمن

### مجلد backups/ فارغ

**الحل:**
- تأكد من أن البوت يعمل بالنسخة الجديدة من `main.py`
- أضف مشترك جديد لتفعيل النظام
- تحقق من Logs في Railway

### الاسترجاع التلقائي لا يعمل

**الحل:**
- تأكد من وجود `backups/users_latest.json`
- تحقق من صلاحيات الملفات
- استخدم `restore_backup.py` يدوياً

---

## 💡 نصائح إضافية

### 1. جدولة النسخ الاحتياطي

استخدم أمر `/backup` بشكل دوري:
- يومياً إذا كان لديك نشاط كبير
- أسبوعياً للاستخدام العادي
- شهرياً للاستخدام الخفيف

### 2. تنظيم النسخ

احفظ النسخ بأسماء واضحة:
```
users_backup_2026-02-19_before_update.json
users_backup_2026-02-20_weekly.json
```

### 3. اختبار الاسترجاع

جرب استرجاع نسخة احتياطية مرة واحدة على الأقل للتأكد من أن كل شيء يعمل.

---

## 📞 الدعم

إذا واجهت أي مشكلة:
- تحقق من Logs في Railway
- استخدم `/me` للتحقق من الإعدادات
- تواصل عبر WhatsApp: [966503560199](https://wa.me/966503560199)

---

## 🎯 الخلاصة

مع هذا النظام الجديد:

✅ **لن تخسر المشتركين** حتى لو حصلت مشاكل في Railway  
✅ **نسخ احتياطية تلقائية** بدون أي جهد منك  
✅ **استرجاع سهل** بأمر واحد أو تلقائياً  
✅ **حماية متعددة الطبقات** للبيانات  

**استخدم البوت بثقة! 🚀**

---

© DDXSTORE
