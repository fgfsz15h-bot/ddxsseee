# 🔄 دليل التحديث السريع

هذا الدليل يشرح كيفية تحديث البوت الحالي إلى النسخة الجديدة مع نظام النسخ الاحتياطي.

---

## ⚡ الخطوات السريعة

### 1. استبدل الملفات

في مستودع GitHub الخاص بك، استبدل الملفات التالية:

| الملف القديم | الملف الجديد | الإجراء |
|-------------|-------------|---------|
| `main.py` | `main.py` (الجديد) | **استبدل** |
| `.gitignore` | `.gitignore` (الجديد) | **استبدل** |
| - | `restore_backup.py` | **أضف** |
| - | `BACKUP_SYSTEM_GUIDE.md` | **أضف** |
| `README.md` | `README.md` (الجديد) | **استبدل** (اختياري) |

### 2. ارفع التحديث

```bash
git add main.py .gitignore restore_backup.py BACKUP_SYSTEM_GUIDE.md README.md
git commit -m "Add automatic backup system"
git push
```

### 3. انتظر إعادة النشر

Railway سيعيد النشر تلقائياً. انتظر حتى ترى "Success" باللون الأخضر.

### 4. اختبر النظام

في تيليجرام:
```
/backup
```

يجب أن يرسل لك البوت ملف JSON.

---

## ✅ ما الذي تغير؟

### في `main.py`

✨ **إضافات جديدة:**
- دالة `_create_backup()` - إنشاء نسخ احتياطية تلقائية
- دالة `_send_backup_to_admin()` - إرسال نسخ للأدمن
- تحديث `_users_load()` - استرجاع تلقائي من النسخ الاحتياطية
- تحديث `_users_save()` - حفظ مع نسخ احتياطي
- أمر جديد `/backup` - إرسال نسخة احتياطية يدوياً
- مجلد جديد `backups/` - لحفظ النسخ الاحتياطية

🔧 **تعديلات:**
- إضافة `from datetime import datetime`
- إضافة متغير `BACKUP_DIR`

✅ **بدون تغيير:**
- جميع الأوامر القديمة تعمل كما هي
- لا حاجة لتغيير المتغيرات البيئية
- المشتركين الحاليين لن يتأثروا

### في `.gitignore`

✨ **إضافات:**
```gitignore
# Data files (except users.json for backup)
data/*.json
!data/users.json

# Backup files (keep directory structure but ignore content)
backups/*.json
```

هذا يسمح برفع `users.json` على GitHub للنسخ الاحتياطي، لكن يتجاهل الملفات الأخرى.

---

## 🎯 الفوائد

### قبل التحديث

❌ إذا أعدت نشر البوت، تُحذف بيانات المشتركين  
❌ لا توجد طريقة لاسترجاع القائمة  
❌ تحتاج لنسخ يدوي دائماً  

### بعد التحديث

✅ نسخ احتياطي تلقائي عند كل عملية حفظ  
✅ استرجاع تلقائي إذا فُقد الملف  
✅ أمر `/backup` للحصول على نسخة فوراً  
✅ الاحتفاظ بآخر 10 نسخ احتياطية  

---

## 🧪 اختبار بعد التحديث

### اختبار 1: النسخ الاحتياطي التلقائي

1. أضف مشترك جديد (من حساب آخر، اضغط `/start`)
2. استخدم `/backup` - يجب أن يرسل لك ملف
3. افتح الملف وتحقق من وجود المشترك الجديد

### اختبار 2: أمر backup

```
/backup
```

يجب أن يرسل لك:
- ملف JSON
- رسالة تحتوي على عدد المشتركين والوقت

### اختبار 3: الأوامر القديمة

تأكد من أن جميع الأوامر القديمة تعمل:
- `/start` ✅
- `/code` ✅
- `/users` ✅
- `/broadcast` ✅

---

## ⚠️ ملاحظات مهمة

### 1. المشتركين الحاليين

✅ **لن يتأثروا!** التحديث يضيف ميزات فقط، لا يحذف شيء.

### 2. Railway

بعد التحديث، Railway سيعيد النشر. هذا طبيعي وسريع (عادة أقل من دقيقة).

### 3. النسخ الاحتياطية القديمة

إذا كان عندك نسخ احتياطية يدوية قديمة، احتفظ بها! يمكنك استخدامها مع `restore_backup.py`.

---

## 🔄 إذا حصلت مشكلة

### البوت لا يعمل بعد التحديث

1. افتح Logs في Railway
2. ابحث عن رسائل الخطأ
3. تأكد من أن `main.py` تم استبداله بالكامل

### أمر /backup لا يعمل

1. استخدم `/me` للتحقق من `ADMIN_ID`
2. تأكد من أنك الأدمن
3. تحقق من Logs

### الاسترجاع للنسخة القديمة

إذا أردت الرجوع للنسخة القديمة:

```bash
git revert HEAD
git push
```

---

## 💡 نصائح

### 1. احفظ نسخة احتياطية قبل التحديث

قبل التحديث، استخدم أمر `/backup` في البوت القديم (إذا كان يعمل) واحفظ الملف.

### 2. اختبر في وقت هادئ

حدّث البوت في وقت لا يكون فيه استخدام كثيف.

### 3. راقب Logs

بعد التحديث، راقب Logs في Railway للتأكد من عدم وجود أخطاء.

---

## 📞 الدعم

إذا واجهت أي مشكلة:
- اقرأ [BACKUP_SYSTEM_GUIDE.md](BACKUP_SYSTEM_GUIDE.md)
- تحقق من Logs في Railway
- تواصل عبر WhatsApp: [966503560199](https://wa.me/966503560199)

---

**بالتوفيق! 🚀**

© DDXSTORE
